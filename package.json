{
  "name": "srn-2016-pt",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "6.8.1"
  },
  "scripts": {
    "start": "pushstate-server client/build $PORT",
    "build": "cd srn-2016-pt && npm run build",
    "add-build-to-git": "git add -f client/build"
  },
  "pre-commit": [
    "build",
    "add-build-to-git"
  ],
  "dependencies": {
    "pushstate-server": "^2.1.0"
  },
  "devDependencies": {
    "pre-commit": "^1.1.3"
  }
}
